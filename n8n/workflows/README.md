# n8n Workflows

This folder is intended for exported n8n workflow JSON files.

Guidelines:
- Do not commit credentials or secrets
- Use placeholder values for endpoints and tokens
- Include a short note per workflow about trigger, expected inputs, and outputs
